# GitHub Packages npm registry configuration for the @global-packages scope
@global-packages:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}

# Optional: force auth for GitHub Packages registry
always-auth=true
